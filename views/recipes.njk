{% extends "layout.njk" %}

{% block content %}

    <section class="cards-recipes">
        {% for recipes in recipes %}
            <div class="card">
                <div class="card-image-recipe" id="">
                    <img src="{{recipes.image}}" alt="{{recipes.title}}">
                </div>
                <div class="card-title-recipe">
                    <p>{{recipes.title}}</p>
                </div>
                <div class="card-author-recipe">
                    <p>por {{recipes.author}}</p>
                </div>
            </div>
        {% endfor %}
    </section>
    
    <script src="scripts.js"></script>

    {% endblock%}