{% extends "layout.njk" %}

{% block content %}

    <section class="cards-receitas">
        {% for recipes in recipes %}
            <div class="card">
                <div class="card-imagem-receita" id="">
                    <img src="{{recipes.image}}" alt="{{recipes.title}}">
                </div>
                <div class="card-titulo-receita">
                    <p>{{recipes.title}}</p>
                </div>
                <div class="card-autor-receita">
                    <p>{{recipes.author}}</p>
                </div>
            </div>
        {% endfor %}
    </section>

    <script src="scripts.js"></script>

    {% endblock%}